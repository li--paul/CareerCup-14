#include <iostream>

int main() {
        std::cout << "Browser sends domain name to the DNS server." << std::endl;
        std::cout << "Browser receives IP address from the DNS server." << std::endl;
        std::cout << "Browser opens TCP connection to the web server at the IP address through port 80." << std::endl;
        std::cout << "Browser sends HTTP request and receives HTML codes." << std::endl;
        std::cout << "Browser renders the HTML page in a window." << std::endl;
}
